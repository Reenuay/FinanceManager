/*
	This scripts represents logics to "Categories" interface.
*/
/*global angular*/
(function () {
	"use strict";
	var app = angular.module("app");
	
	app.controller("CategoriesAddController", ["$scope", "$firebaseArray", "$userData", "Notification", "$error", function ($scope, $http, $firebaseArray, $userData, Notification, $error) {
		
		/*
			Scope variables.
		*/
		$scope.main = {};
	}]);
}());
